spring:
  cloud:
    config:
      name: menu #对应的配置文件名称
      label: master #git仓库分支名
      uri: http://localhost:8888 #配置中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/